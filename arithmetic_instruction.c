#include <stdio.h>
int main (){
    int a,b, sum;
    printf("enter no a:");
    scanf("%d",&a);
     printf("enter no b:");
    scanf("%d",&b);
    sum=a+b;
    printf("sum is equal to: %d\n",sum);
   int multiply;
   multiply=a*b;
   printf("product is equal to: %d\n",multiply);

    return 0;

}